import React from "react";

type TableProps = {
  data: Array<Record<string, unknown>>;
};

export default function MLBTable({ data }: TableProps) {
  const { } = data;
  return (
    <table>
      <thead>
        <tr>
          { }
        </tr>
      </thead>
      <tbody>

      </tbody>
    </table>
  );
}